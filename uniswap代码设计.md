### 1.在构造的时候利用Create2进行创角池子， 预先知道地址，链下可验证，且计算时也无需去跨合约调用。

### 2.uniswapV3 LP在取款时，将记账和转账分离， 可以独立取回手续费，也可以分币burn，然后统一取回代币。 节省Gas，方便用户。 因为Transfer是远贵于存储状态。

### 3.FlashSwap 闪电交易， 原子交易设计，属于开始乐观处理，要什么给什么 ，中间放事件由外部实现，结尾验证状态。 既安全，又灵活。 

### 4.节省Gas尽量少存储，多计算， 对于频繁调用的函数，多使用 named return变量， 错误多使用error。  对于操作频繁的结构体，尽量放在一个slot中，32字节。

### 5.V3 在计算tick到sqrt(price)*X96
方法： getSqrtRatioAtTick（）

price = 1.0001^tick

sqrtPrice = 1.0001^(tick/2)

sqrtPriceX96 = 1.0001^(tick/2) * 2^96

利用二进制拆分的设计：
tick = 199 = 128 + 64 + 4 + 2 + 1 = 2^7 + 2^6 + 2^2 + 2^1 + 2^0
1.0001^199 = 1.0001^128 * 1.0001^64 * 1.0001^4 * 1.0001^2 * 1.0001^1
1.0001^1   = 已知常数
1.0001^2   = 已知常数
1.0001^4   = 已知常数
1.0001^8   = 已知常数
...
1.0001^131072 = 已知常数
所以计算任意 tick 对应的价格，最多只需要 19 次乘法（因为 tick 最大 ~887272 < 2^20）。

### 6. 



